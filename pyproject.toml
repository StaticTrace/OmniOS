[project]
name = "omnios-hub"
version = "1.0.0"
description = "A self-hosted personal web OS — modular dark-mode dashboard built with Python/Flask and vanilla JavaScript"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
    "python-dotenv>=1.2.2",
    "requests>=2.33.1",
]
